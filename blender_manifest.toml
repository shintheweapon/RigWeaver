schema_version = "1.0.0"

id = "armature_extractor"
version = "0.1.0"
name = "Armature Extractor"
tagline = "Extract a reduced armature containing only deforming bones"
maintainer = "theweapon"
type = "add-on"

blender_version_min = "4.5.0"

license = ["SPDX:GPL-3.0-or-later"]

tags = ["Rigging", "Armature"]
